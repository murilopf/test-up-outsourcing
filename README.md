# Para testar é muito fácil

 - Clone o projeto em sua maquina
 - Instale as depedencias com `yarn` ou `npm`
 - Pronto, agora é só você executar o projeto no seu terminal com o comando:

- ### `yarn start` ou `npm start`

#